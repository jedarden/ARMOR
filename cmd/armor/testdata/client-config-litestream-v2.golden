# Litestream configuration for ARMOR
# Add to litestream.yml or use as environment variables

# Litestream environment variables
LITESTREAM_ENDPOINT=http://armor.example.com:9000
LITESTREAM_REGION=us-east-1  # Required but unused by ARMOR

# Credentials
# Set these from your ARMOR_AUTH_ACCESS_KEY_ID and ARMOR_AUTH_SECRET_KEY
LITESTREAM_ACCESS_KEY_ID=YOUR_ACCESS_KEY_ID
LITESTREAM_SECRET_ACCESS_KEY=YOUR_SECRET_ACCESS_KEY

# Or in litestream.yml:
dbs:
  - path: /path/to/db.sqlite
    replicas:
      - type: s3
        endpoint: http://armor.example.com:9000
        bucket: YOUR_BUCKET
        region: us-east-1
        access-key-id: YOUR_ACCESS_KEY_ID
        secret-access-key: YOUR_SECRET_ACCESS_KEY

# Multipart (write format v2, legacy; ADR-015): part 1 pins the uniform
# part size P; non-final parts must be exactly P, the final part any size.
# A part numbered >1 arriving before part 1 is deferred with retryable
# 503 SlowDown -- default client retry clears it. No tuning required.
# litestream's multipart snapshots work unmodified on both formats; no serial knob exists or is needed.
# Tested compatibility matrix: docs/multipart-client-compatibility.md
