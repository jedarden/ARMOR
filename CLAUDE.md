# ARMOR

@AGENTS.md

`AGENTS.md` above is the canonical guide for every agent working in this
repository: layout, gates, beads, commits, releases, deployments. The
home-directory `CLAUDE.md` (org-wide prohibitions and cluster access) still
applies; nothing in this repository overrides it.
