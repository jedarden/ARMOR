apiVersion: v1
kind: Service
metadata:
  name: restore-verifier
  labels:
    app: restore-verifier
spec:
  type: ClusterIP
  ports:
  - port: 9002
    targetPort: 9002
    protocol: TCP
    name: http
  selector:
    app: restore-verifier
