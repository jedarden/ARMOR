# Bead bf-1pbu: which Command Existence Check

## Task
Check if the `which` command exists on the system.

## Result
The `which` command exists and is available at `/usr/bin/which`.

## Details
- Command executed: `which which`
- Output: `/usr/bin/which`
- Exit code: 0 (success)

## Conclusion
The `which` utility is installed and functional on this system.
