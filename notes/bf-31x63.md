# Bead bf-31x63: Fix int64 Negative Conversion Test Cases

## Summary
Fixed basic negative conversion test cases in `int64_to_uint64_negative_conversion_test.go` to match the int32 pattern.

## Changes Made
- Updated `expectedInMsg` arrays in `TestInt64ToUint64NegativeConversion` to match int32 pattern
- Simplified error message expectations from `[]string{"cannot unmarshal", "-<value>"}` to `[]string{"cannot unmarshal"}`
- Applied this pattern to all intermediate negative value test cases
- Kept special edge cases (-1 and minimum value -9223372036854775808) with value-specific expectations

## Pattern Alignment
The int32 pattern uses:
- Edge cases (-1, min value): `expectedInMsg: []string{"cannot unmarshal", "-<value>"}`
- Other negative values: `expectedInMsg: []string{"cannot unmarshal"}`
- Positive values: no expectedInMsg (shouldError: false)

Updated int64 test cases to follow the same pattern for consistency.

## Test Results
All tests pass successfully:
- 22 test cases in TestInt64ToUint64NegativeConversion
- All negative conversions produce expected errors
- All positive conversions succeed correctly
- Error message quality verified

## Files Modified
- `internal/yamlutil/int64_to_uint64_negative_conversion_test.go`
