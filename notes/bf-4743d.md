# Bead bf-4743d: Verify kubeconfig path exists and is accessible

## Task
Check that the kubeconfig file for ord-devimprint exists at the expected path and is readable.

## Finding
The kubeconfig file `/home/coding/.kube/ord-devimprint.kubeconfig` does not exist.

## Explanation
This is expected behavior. According to the infrastructure documentation:

- **ord-devimprint** uses kubectl-proxy over Tailscale for cluster access
- Access is read-only via the proxy pod running in the `devpod-observer` namespace
- The proxy is exposed via Tailscale operator at `kubectl-proxy-ord-devimprint:8001`
- No direct kubeconfig file exists for this cluster

## Correct Access Method
To access ord-devimprint:

```bash
kubectl --server=http://kubectl-proxy-ord-devimprint:8001 get pods -n <namespace>
```

## Clusters with Direct Kubeconfig
The following clusters have direct kubeconfig files:
- `ardenone-manager` - `/home/coding/.kube/ardenone-manager.kubeconfig`
- `rs-manager` - `/home/coding/.kube/rs-manager.kubeconfig`
- `iad-ci` - `/home/coding/.kube/iad-ci.kubeconfig`
- `iad-options` - `/home/coding/.kube/iad-options.kubeconfig` (read/write) and `iad-options-observer.kubeconfig` (read-only)

All other clusters (apexalgo-iad, ardenone-cluster, ardenone-hub, ord-devimprint, iad-kalshi) use kubectl-proxy over Tailscale.
