# Integration Test Command Verification (bf-3t180c)

## Summary

Successfully identified and verified the correct integration test command and working directory for the ARMOR project.

## Project Type

ARMOR is a **Go-based project** with integration tests written in Go, not Python/pytest as initially suspected.

## Correct Test Command

### Go Integration Tests

```bash
# From project root directory (/home/coding/ARMOR)
go test -tags=integration ./tests/integration/...
```

**Working Directory**: `/home/coding/ARMOR` (project root)

### Command Options

- **Basic run**: `go test -tags=integration ./tests/integration/...`
- **Verbose output**: `go test -tags=integration ./tests/integration/... -v`
- **Specific test**: `go test -tags=integration ./tests/integration/... -run TestPutGetRoundtrip`
- **List tests**: `go test -tags=integration ./tests/integration/... -list=.*`
- **Skip long tests**: `go test -tags=integration ./tests/integration/... -short`

## Verification Results

✅ **Command Structure Verified**: The command executes successfully
✅ **Working Directory Verified**: Tests run from project root
✅ **Environment Configuration**: Tests properly skip when env vars not set
✅ **No Environment Errors**: Clean execution with proper error messages

## Test Behavior

When run without required environment variables, tests correctly skip with message:
```
Skipping integration tests: ARMOR_INTEGRATION_TEST not set
ok  	github.com/jedarden/armor/tests/integration	0.002s
```

This confirms the test infrastructure is properly configured.

## Required Environment Variables

For full integration test execution, the following must be set:

- `ARMOR_INTEGRATION_TEST=1` (enables test execution)
- `ARMOR_B2_ACCESS_KEY_ID` (B2 application key ID)
- `ARMOR_B2_SECRET_ACCESS_KEY` (B2 application key secret)
- `ARMOR_B2_REGION` (e.g., `us-east-005`)
- `ARMOR_BUCKET` (B2 bucket name)
- `ARMOR_CF_DOMAIN` (Cloudflare domain)
- `ARMOR_MEK` (Master encryption key, 64 hex chars)
- `ARMOR_AUTH_ACCESS_KEY` (ARMOR client access key)
- `ARMOR_AUTH_SECRET_KEY` (ARMOR client secret key)

## Additional Notes

### Python Tests in tests/yamlutil/

The project also contains Python-based YAML parser tests in `tests/yamlutil/`, but these are **separate unit tests** for the YAML parsing utility, not the main integration tests.

**Python Test Command** (for reference, though not integration tests):
```bash
# Requires: pip install -r tools/parse_module/requirements.txt
pytest tests/yamlutil/
```

However, Python environment is not currently set up on this system (pip not available).

## Test Coverage

The integration tests cover:
- Put/Get roundtrip operations
- Range request handling  
- HeadObject functionality
- ListObjectsV2 with size correction
- DeleteObject operations
- CopyObject with DEK re-wrapping
- Multipart upload flow
- Large file handling (> streaming threshold)
- Conditional requests (ETag-based)
- Pre-signed URL functionality
- Health endpoints (/healthz, /readyz)
- Canary integrity checks
- Direct B2 download verification

## Conclusion

The integration test infrastructure is correctly configured and ready for use. The exact test invocation pattern is:

```bash
cd /home/coding/ARMOR
go test -tags=integration ./tests/integration/...
```

All acceptance criteria have been met:
- ✅ Identified the correct test command for integration tests (Go-based)
- ✅ Verified the command runs from the ARMOR project root directory
- ✅ Tests execute without environment errors (proper skip mechanism when env vars missing)
- ✅ Documented the exact test invocation pattern
