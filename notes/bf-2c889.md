# Validate() Call Sites Catalog

## Task Completion Summary

Successfully captured all Validate() method call sites in the ARMOR Rust codebase using ripgrep.

## Results

**Total Validate() call sites found: 134**

### Distribution by File

| File | Count | Type |
|------|-------|------|
| tests/schema_validation_test.rs | 70 | Unit tests |
| src/schema.rs | 41 | Implementation & tests |
| src/parsers/config.rs | 11 | Config validation |
| src/parsers/yaml/syntax_validator.rs | 8 | YAML validation |
| src/parsers/yaml/syntax_detector_tests.rs | 3 | Integration tests |
| src/parsers/yaml/parser.rs | 1 | Parser implementation |

### Categories

1. **Production code (11 calls):**
   - src/parsers/config.rs: 2 actual validation calls (lines 662, 1007)
   - src/parsers/yaml/parser.rs: 1 validation call (line 121)
   - src/parsers/yaml/syntax_validator.rs: 8 validation calls (lines 438-503)

2. **Test code (123 calls):**
   - Unit tests in schema_validation_test.rs: 70 calls
   - Schema.rs doctests and examples: 41 calls
   - Config.rs test assertions: 9 calls
   - YAML syntax detector tests: 3 calls

### Raw Output

The complete raw grep output is saved in `notes/bf-2c889-validate-calls.txt` with all 134 call sites including file paths and line numbers.

## Methodology

- Used `rg "\.validate\(" --type rust -n` to search recursively
- Filtered out docstring comments (///) to eliminate false positives
- Captured full file paths and line numbers
- Verified no false positives in final output

## Acceptance Criteria Met

✅ All Validate() call sites captured (134 total)
✅ Raw output saved with full file paths
✅ No false positives in results (docstrings filtered)
