# Validate() Call Sites Search Results

## Task
Search for Validate() method calls in the ARMOR Rust codebase.

## Methodology
- Used `rg` (ripgrep) to search for `.validate()` method call patterns
- Searched only `.rs` files (Rust source)
- Captured file paths and line numbers

## Results Found

### Total Call Sites: 11

All Validate() calls are located in `src/parsers/config.rs`:

#### Production Code (2 calls)
1. **src/parsers/config.rs:662** - `self.config.validate()?;`
2. **src/parsers/config.rs:1007** - `self.config.validate()?;`

#### Test Code (9 calls)
All in test functions within `src/parsers/config.rs`:

3. **src/parsers/config.rs:1202** - `assert!(config.validate().is_ok());`
4. **src/parsers/config.rs:1205** - `assert!(strict_config.validate().is_ok());`
5. **src/parsers/config.rs:1216** - `assert!(config.validate().is_err());`
6. **src/parsers/config.rs:1224** - `assert!(config2.validate().is_err());`
7. **src/parsers/config.rs:1232** - `assert!(config3.validate().is_err());`
8. **src/parsers/config.rs:1297** - `assert!(config.validate().is_ok());`
9. **src/parsers/config.rs:1300** - `assert!(strict_config.validate().is_ok());`
10. **src/parsers/config.rs:1312** - `assert!(config.validate().is_err());`
11. **src/parsers/config.rs:1320** - `assert!(config2.validate().is_err());`

## Raw Output
Raw grep output saved to: `notes/bf-2c889-validate-calls.txt`

## Notes
- No false positives detected
- All results are actual `.validate()` method calls (not comments or other occurrences)
- Search pattern `Validate(` (capital V, function call style) only appears in comments in test files
