# Bead bf-4s109h: Define ScopeInfo struct for stack entries

## Task Verification

The `ScopeInfo` struct was already implemented in `src/parsers/yaml/scope.rs` (lines 184-391).

## Acceptance Criteria - All Met

1. ✅ ScopeInfo struct exists in src/parsers/yaml/scope.rs
2. ✅ Struct has fields to track scope type (block, flow, etc.)
   - `scope_type: ScopeType` enum supporting:
     - Block (indentation-based nesting)
     - FlowMapping (inline `{key: value}` syntax)
     - FlowSequence (inline `[item1, item2]` syntax)
     - BlockSequence (dash-prefixed items)
     - Root (document root)
3. ✅ Struct has fields to track scope depth
   - `scope_depth: usize` (0-based, root = 0, top-level = 1)
4. ✅ Struct is documented with clear purpose
   - Comprehensive module-level documentation
   - Detailed struct-level doc comments explaining purpose
   - Field descriptions
   - Usage examples
   - Method documentation

## Implementation Details

**Constructor Methods:**
- `new(scope_type, scope_depth)` - Create with explicit values
- `root()` - Create root scope info (depth 0)
- `block(scope_depth)` - Create block-style scope
- `flow_mapping(scope_depth)` - Create flow mapping scope
- `block_sequence(scope_depth)` - Create block sequence scope

**Type Checking Methods:**
- `is_root()` - Check if root scope
- `is_block()` - Check if block-style scope
- `is_flow()` - Check if flow-style scope
- `is_sequence()` - Check if sequence scope
- `is_mapping()` - Check if mapping scope

**Depth Management:**
- `scope_depth()` - Get current depth
- `set_scope_depth()` - Set depth explicitly
- `increment_depth()` - Increment by 1
- `decrement_depth()` - Decrement by 1 (with floor at 0)

**Display Support:**
- Implements `Display` for human-readable output
- Implements `Debug` for debugging
- `describe()` method for string representation

## Status

The implementation is complete and compiles successfully. This is a data structure definition only, with no parser integration yet (as specified in scope).
