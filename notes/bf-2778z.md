# Task bf-2778z: Retrieve and decode LITESTREAM_ACCESS_KEY_ID

## Status: BLOCKER - Prerequisite kubeconfig not available

## Summary
This task requires retrieving the `LITESTREAM_ACCESS_KEY_ID` value from the `armor-writer` secret in the `devimprint` namespace on the `ord-devimprint` cluster.

## Secret Details (from deployment config)
Based on `/home/coding/declarative-config/k8s/ord-devimprint/devimprint/queue-api-deployment.yml`, the environment variable mapping is:
- `LITESTREAM_ACCESS_KEY_ID` → secret `armor-writer`, field `auth-access-key`
- `LITESTREAM_SECRET_ACCESS_KEY` → secret `armor-writer`, field `auth-secret-key`

The secret is managed via ExternalSecret from OpenBao path: `rs-manager/ord-devimprint/armor-writer`

## Blocker: No kubeconfig with secret access
The ord-devimprint cluster is only accessible via read-only kubectl-proxy:
- Proxy: `http://kubectl-proxy-ord-devimprint:8001`
- ServiceAccount: `system:serviceaccount:devpod-observer:devpod-observer`
- Permissions: Read-only, explicitly denies access to secrets

### Available kubeconfigs
Only two kubeconfigs exist on this system:
1. `~/.kube/iad-acb.kubeconfig` - Points to iad-acb cluster (via http://traefik-iad-acb:8001)
2. `~/.kube/iad-ci.kubeconfig` - Points to iad-ci cluster (Rackspace Spot cluster)

Neither provides access to the devimprint namespace on ord-devimprint with secret permissions.

### Attempted commands (all failed with Forbidden)
\`\`\`bash
# Read-only proxy - no secret access
kubectl --server=http://kubectl-proxy-ord-devimprint:8001 get secret armor-writer -n devimprint
# Error: User "system:serviceaccount:devpod-observer:devpod-observer" cannot get resource "secrets"

# iad-acb kubeconfig - different cluster, read-only
kubectl --kubeconfig=/home/coding/.kube/iad-acb.kubeconfig get secret armor-writer -n devimprint
# Error: User "system:serviceaccount:devpod-observer:devpod-observer" cannot get resource "secrets"

# iad-ci kubeconfig - different cluster
kubectl --kubeconfig=/home/coding/.kube/iad-ci.kubeconfig get secret armor-writer -n devimprint
# Error: namespaces "devimprint" not found
\`\`\`

## Prerequisite Status
❌ **FAILED** - The stated prerequisite "Previous child beads complete (kubeconfig works, secret exists)" is NOT satisfied. No kubeconfig exists with permissions to read secrets from the devimprint namespace.

## Resolution Required
To complete this task, one of the following must be provided:
1. A kubeconfig file with secret access to the ord-devimprint cluster
2. Direct access to the OpenBao secret at `rs-manager/ord-devimprint/armor-writer`
3. An alternative method to retrieve the secret value

## Command (once kubeconfig is available)
\`\`\`bash
kubectl --kubeconfig=<path-to-kubeconfig> get secret armor-writer -n devimprint -o jsonpath='{.data.auth-access-key}' | base64 -d
\`\`\`

## References
- Deployment config: `/home/coding/declarative-config/k8s/ord-devimprint/devimprint/queue-api-deployment.yml` (lines 111-120)
- ExternalSecret config: `/home/coding/declarative-config/k8s/ord-devimprint/devimprint/devimprint-externalsecrets.yml` (lines 52-77)
- CLAUDE.md Kubernetes access section documents read-only nature of ord-devimprint proxy
