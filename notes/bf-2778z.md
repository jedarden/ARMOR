# LITESTREAM_ACCESS_KEY_ID Retrieval Attempt - Blocker Persists

## Date: 2026-07-11 (Re-verified - blocker persists)

## Issue
Unable to retrieve `LITESTREAM_ACCESS_KEY_ID` from `armor-writer` secret in `devimprint` namespace on `ord-devimprint` cluster.

## Root Cause
**No kubeconfig exists with secret access to ord-devimprint cluster.**

### Available Access Methods
1. **Read-only proxy** (`http://kubectl-proxy-ord-devimprint:8001`)
   - Uses ServiceAccount: `system:serviceaccount:devpod-observer:devpod-observer`
   - RBAC explicitly denies secret access
   - Error: `User "system:serviceaccount:devpod-observer:devpod-observer" cannot get resource "secrets"`

2. **Admin kubeconfigs**
   - `/home/coding/.kube/ardenone-manager.kubeconfig` - DOES NOT EXIST
   - `/home/coding/.kube/iad-ci.kubeconfig` - Only has iad-ci cluster access
   - `/home/coding/.kube/iad-acb.kubeconfig` - Unknown scope

### Investigation Results
```bash
# Secret exists and is synced from OpenBao
kubectl --server=http://kubectl-proxy-ord-devimprint:8001 get externalsecrets -n devimprint
# armor-writer   ClusterSecretStore   openbao   1h   SecretSynced   True

# But secret access is blocked by RBAC
kubectl --server=http://kubectl-proxy-ord-devimprint:8001 get secret armor-writer -n devimprint
# Error: Forbidden - cannot get resource "secrets"
```

## Prerequisite Status
**INCOMPLETE** - The bead description states "Previous child beads complete (kubeconfig works, secret exists)" but no kubeconfig with secret access to ord-devimprint has been provisioned.

## Required Action
Before this bead can be completed, an admin kubeconfig for ord-devimprint cluster must be created and saved to `/home/coding/.kube/ord-devimprint.kubeconfig` with:
- Cluster-admin or secret-reader RBAC
- Ability to retrieve secrets from `devimprint` namespace

## Historical Context
This is a recurring blocker across multiple beads:
- bf-2778z: Current bead - LITESTREAM_ACCESS_KEY_ID retrieval
- bf-112tt: SECRET_ACCESS_KEY retrieval blocker documented
- bf-5vow9: armor-writer secret verification blocker

All are blocked by the same underlying issue: no admin access to ord-devimprint cluster.
