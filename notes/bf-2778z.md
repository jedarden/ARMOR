# Bead bf-2778z: Blocker Documentation

## Task
Retrieve and decode LITESTREAM_ACCESS_KEY_ID from the armor-writer secret in the devimprint namespace.

## Blocker
**Prerequisite bead bf-2p1wr is OPEN (not complete)**

### Details
1. **Required kubeconfig missing**: The task requires a kubeconfig with write access to ord-devimprint cluster to read secrets, but bead bf-2p1wr (obtain kubeconfig) is still open.

2. **Available access only**: Read-only kubectl proxy at `kubectl-proxy-ord-devimprint:8001` which explicitly denies secret access:
   ```
   Error: User "system:serviceaccount:devpod-observer:devpod-observer" cannot get resource "secrets"
   ```

3. **No rs-manager access**: The OpenBao path is `rs-manager/ord-devimprint/armor-writer`, suggesting the secret originates from rs-manager's OpenBao instance. However:
   - No kubeconfig exists at `/home/coding/.kube/rs-manager.kubeconfig`
   - Cannot access rs-manager cluster

### Secret Structure (from ExternalSecret definition)
The `armor-writer` ExternalSecret (k8s/ord-devimprint/devimprint/devimprint-externalsecrets.yml) specifies:
- **Secret keys**: `auth-access-key`, `auth-secret-key`
- **OpenBao path**: `rs-manager/ord-devimprint/armor-writer`
- **Deployment env vars**: `LITESTREAM_ACCESS_KEY_ID` (maps to auth-access-key), `LITESTREAM_SECRET_ACCESS_KEY` (maps to auth-secret-key)

**Note**: Task description mentions `LITESTREAM_ACCESS_KEY_ID` as the secret key, but that's actually the environment variable name. The actual secret key is `auth-access-key`.

### Resolution Required
Bead bf-2p1wr must be completed first to obtain a kubeconfig with secret access to ord-devimprint cluster. This may require:
1. Coordinating with cluster administrator
2. Creating/obtaining ~/.kube/ord-devimprint.kubeconfig or ~/.kube/rs-manager.kubeconfig
3. Verifying the kubeconfig has permissions to read secrets in devimprint namespace

### Verification
As of 2026-07-11:
- `bf-2p1wr` (Obtain ord-devimprint kubeconfig) - **OPEN**
- `bf-4ds4n` (Verify kubeconfig exists) - **CLOSED** (closed prematurely despite open prerequisite)
- `bf-5vow9` (Verify secret exists) - **CLOSED** (completed via ExternalSecret status workaround)
- `bf-2778z` (current) - **IN_PROGRESS** but blocked by bf-2p1wr

## Next Steps
1. Complete bead bf-2p1wr to obtain required kubeconfig
2. Re-attempt bf-2778z with proper credentials
3. Alternative: Access secret directly from OpenBao if rs-manager access becomes available
