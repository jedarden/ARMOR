# Bead bf-3s75w1: Litestream Restore Prerequisites Verification

**Date:** 2026-08-01
**Bead ID:** bf-3s75w1
**Status:** CREDENTIAL+ENDPOINT GATES CONFIRMED
**Related Bead:** bf-34xw9 (22+ failed attempts on same gates)

## Executive Summary

Verified all prerequisites for executing litestream restore. Found **two critical blockers** that match the historical gates documented in bf-34xw9:

1. **CREDENTIAL GATE** - No B2 credentials available (environment variables empty, RBAC blocks secret access)
2. **ENDPOINT GATE** - Cannot reach ARMOR service (neither `armor:9000` nor `100.80.255.8:9000` accessible)

## Prerequisite Verification Results

### ✅ PASS: Scratch Directory

```bash
ls -la ~/scratch/
# Status: EXISTS, drwxr-xr-x permissions, owned by coding:users

# Write test
touch ~/scratch/test-write-$$ && rm ~/scratch/test-write-$$
# Result: ✅ SUCCESS - directory is writable
```

**Conclusion:** Scratch directory `/home/coding/scratch/` is ready for restore operations.

### ✅ PASS: Litestream Binary

```bash
which litestream
# Result: /home/coding/.local/bin/litestream

litestream version
# Result: (development build)
```

**Conclusion:** Litestream CLI is installed and executable.

### ❌ FAIL: B2 Credentials

#### Environment Variables Check
```bash
echo $B2_KEY_ID_FILE
# Result: (empty)

echo $B2_SECRET_ACCESS_KEY_FILE
# Result: (empty)
```

#### Kubernetes Secrets Check (Read-Only Proxy)
```bash
kubectl --server=http://traefik-ardenone-manager:8001 \
  get secrets -n armor armor-secrets -o jsonpath='{.data}'
# Result: (no output - RBAC blocks secret access)
```

#### Related Bead Context
- **bf-24hrg** (credential acquisition) - Status: CLOSED (2026-07-14)
- **bf-34xw9** (actual restore) - Status: OPEN, 22+ attempts blocked on empty SECRET_ACCESS_KEY

**Conclusion:** 
- No environment credentials are set
- Read-only kubectl proxy blocks secret access
- Historical credential cache from bf-24hrg was empty (0 bytes for SECRET_ACCESS_KEY)
- **CREDENTIAL GATE CONFIRMED**

### ❌ FAIL: Endpoint Connectivity

#### In-Cluster Endpoint Test
```bash
nc -zv armor:9000
# Result: Hostname not resolvable from this host
```

#### External Endpoint Test
```bash
nc -zv 100.80.255.8 9000
# Result: Failed to connect to 100.80.255.8:9000
```

#### Kubernetes Service Check (Read-Only)
```bash
kubectl --server=http://traefik-ardenone-manager:8001 \
  get svc armor -n armor -o yaml
# Result: (no output - service may not exist or RBAC blocks)
```

**Conclusion:** 
- Cannot reach ARMOR S3 endpoint from this server
- Neither in-cluster hostname nor external IP is accessible
- **ENDPOINT GATE CONFIRMED**

## Historical Context: bf-34xw9 (22+ Failed Attempts)

From `notes/bf-34xw9-blocker-summary-2026-07-14.md`:

- **Attempts:** 19 consecutive failures on 2026-07-14 alone
- **Blocker:** Empty SECRET_ACCESS_KEY (0 bytes) despite bf-24hrg being marked closed
- **Root cause:** Read-only proxy prevents secret access, no write kubeconfig for ord-devimprint
- **Infrastructure:** 100% ready (scratch, litestream, config all verified)
- **Time to complete (if unblocked):** ~5 minutes

The current verification confirms **the same two gates persist**.

## Comparison to bf-34xw9 Environment

| Component | bf-34xw9 State (2026-07-14) | Current State (2026-08-01) |
|-----------|----------------------------|----------------------------|
| Scratch directory | ✅ READY | ✅ READY |
| Litestream binary | ✅ READY | ✅ READY |
| ACCESS_KEY_ID | ⚠️ CACHED (available) | ❌ NOT SET |
| SECRET_ACCESS_KEY | ❌ MISSING (0 bytes) | ❌ NOT SET |
| Endpoint connectivity | ❌ 100.80.255.8:9000 unreachable | ❌ Same |
| Secret access (RBAC) | ❌ Read-only blocked | ❌ Same |

**Conclusion:** The gates that blocked bf-34xw9 remain unchanged.

## What Would Be Required to Unblock

### Option 1: Direct Credential Provision
```bash
# Requires write access to ord-devimprint cluster
kubectl --kubeconfig=<write-kubeconfig> \
  get secret armor-writer -n devimprint \
  -o jsonpath='{.data.auth-access-key}' | base64 -d

kubectl --kubeconfig=<write-kubeconfig> \
  get secret armor-writer -n devimprint \
  -o jsonpath='{.data.auth-secret-key}' | base64 -d
```

### Option 2: Network Access
```bash
# Requires network route to ARMOR service
# Either VPN route, Tailscale, or kubectl port-forward
kubectl --kubeconfig=<write-kubeconfig> \
  port-forward -n armor svc/armor 9000:9000
```

### Option 3: In-Cluster Restore Job
```bash
# Requires write access to create Job in ord-devimprint
kubectl --kubeconfig=<write-kubeconfig> \
  create -f litestream-restore-job.yaml
```

**All options require write access that is not currently available.**

## Security Context

The RBAC restrictions are **correct security design**:
- ✅ Prevents accidental secret exposure
- ✅ Follows principle of least privilege  
- ✅ Protects production infrastructure
- ✅ This is how the system should work

The gates are **features, not bugs**.

## Acceptance Criteria Status

| Criteria | Status | Evidence |
|----------|--------|----------|
| Confirmed scratch directory exists and is writable | ✅ COMPLETE | Verified with test file create/delete |
| Verified litestream binary is available and executable | ✅ COMPLETE | Found at `~/.local/bin/litestream` |
| Checked B2 credentials/access | ❌ GATE | Environment vars empty, RBAC blocks secrets |
| Verified endpoint connectivity | ❌ GATE | Neither `armor:9000` nor `100.80.255.8:9000` reachable |
| Documented any credential/access issues discovered | ✅ COMPLETE | This note |

## Recommendations

### For This Bead (bf-3s75w1)
- **CLOSE as COMPLETE** - Prerequisite verification done, gates documented
- All acceptance criteria met (documenting the gates IS the task)

### For Related Work
- **Do NOT retry bf-34xw9** - Gates remain, 22+ attempts sufficient
- **Consider bf-34xw9 OBSOLETE** - Premise may be outdated (queue-api moved to commitgraph namespace per memory)
- **Address bf-24hrg closure issue** - Bead marked closed but credentials were incomplete
- **Plan credential refresh mechanism** - Current approach (manual secret access) doesn't scale

## Technical Details

### Verification Commands Used
```bash
# Scratch directory
ls -la ~/scratch/
touch ~/scratch/test-write-$$ && rm ~/scratch/test-write-$$

# Litestream binary
which litestream && litestream version

# Environment credentials
echo $B2_KEY_ID_FILE && echo $B2_SECRET_ACCESS_KEY_FILE

# Secret access (RBAC)
kubectl --server=http://traefik-ardenone-manager:8001 \
  get secrets -n armor armor-secrets -o jsonpath='{.data}'

# Endpoint connectivity
timeout 5 nc -zv 100.80.255.8 9000
nc -zv armor:9000

# Service discovery
kubectl --server=http://traefik-ardenone-manager:8001 \
  get svc -n armor
```

### Environment Context
- **Server:** Hetzner EX44 (standalone, not a pod)
- **Primary cluster:** ardenone-manager (read-only proxy)
- **Target cluster:** ord-devimprint (where credentials and backup live)
- **Access method:** kubectl-proxy over Tailscale (read-only)
- **Write access:** No ord-devimprint kubeconfig with write permissions

## Related Documentation

### Memory Files
- `memory/bf-34xw9-litestream-restore-gated.md` - Primary gate documentation
- `memory/bf-5aqh0-queue-api-restore-obsolete.md` - Premise obsolescence note

### Historical Notes
- `notes/bf-34xw9-blocker-summary-2026-07-14.md` - 19-attempt failure analysis
- `notes/bf-34xw9-attempt-*-2026-07-14.md` - Individual attempt logs
- `notes/bf-34xw9-restore-procedure-guide.md` - Restore procedure (blocked on credentials)

## Conclusion

**All prerequisites for litestream restore have been verified.**

The verification confirms **two persistent gates** that blocked 22+ previous attempts:
1. **CREDENTIAL GATE** - No B2 credentials accessible (RBAC + empty cache)
2. **ENDPOINT GATE** - ARMOR service unreachable (network + routing)

These gates are **security features, not bugs**. The infrastructure (scratch, litestream) is ready, but operational access is intentionally restricted.

This bead (bf-3s75w1) should **close complete** - its purpose was to verify prerequisites and document gates, which is now done.

---

**Bead ID:** bf-3s75w1
**Document Version:** 1.0
**Last Updated:** 2026-08-01
**Author:** Claude Code (claude-code-glm-4.7-roam7)
