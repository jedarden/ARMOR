# Bead bf-651m0: Update core validation test files to pass path parameter

## Status: Already Completed

The work for this bead was already completed in commit `1082b97b` on 2026-07-12 01:38:19.

## Summary of Changes (from commit 1082b97b)

Updated 4 `NewValidationError` calls in `internal/yamlutil/validation_error_path_test.go` to pass the path parameter (using `tt.fieldPath`) instead of empty string:

- `TestValidationErrorPathFormatting_WithLineAndColumn`
- `TestValidationErrorPathFormatting_ExactFormat`
- `TestValidationErrorPathFormatting_StringMethod`
- `TestValidationErrorPathFormatting_RealWorldExamples`

## Verification

All three target files were checked and confirmed to have all `NewValidationError` calls passing the path parameter:

### internal/yamlutil/errors_test.go
- 8 `NewValidationError` calls - all passing path parameter ✓
- Tests use `tt.fieldPath`, `"server.port"`, `""` (for empty paths), and `"database.connectionTimeout"`

### internal/yamlutil/validation_error_path_test.go
- 9 `NewValidationError` calls - all passing path parameter ✓
- Tests use `tt.fieldPath` for all test cases

### internal/yamlutil/error_message_quality_test.go
- 10 `NewValidationError` calls - all passing path parameter ✓
- Tests use `"server.port"`, `"field"`, or `"f"` as appropriate

## Test Results

All yamlutil tests pass:
```
ok  	github.com/jedarden/armor/internal/yamlutil	0.037s
```

## Acceptance Criteria Met

- ✓ All `NewValidationError` calls in the 3 files pass path parameter
- ✓ Path values reflect the actual validation error location (using `fieldPath` value)
- ✓ Tests still pass after changes
