# bf-2y15n: BLOCKER - Infrastructure Access Missing (2026-07-11)

## Task Status: BLOCKED

Task: Retrieve base64-encoded `LITESTREAM_ACCESS_KEY_ID` from armor-writer secret in devimprint namespace

## Blocker Details

### Required Access Missing

1. **ord-devimprint kubeconfig** (`/home/coding/.kube/ord-devimprint.kubeconfig`)
   - Required by task description for direct cluster access
   - Does NOT exist on system
   - Verification: `ls -la /home/coding/.kube/ord-devimprint.kubeconfig` → "No such file or directory"

2. **rs-manager kubeconfig** (`/home/coding/.kube/rs-manager.kubeconfig`)
   - Would provide alternative access to OpenBao at `rs-manager/ord-devimprint/armor-writer`
   - Does NOT exist on system
   - Verification: `ls -la /home/coding/.kube/rs-manager.kubeconfig` → "No such file or directory"

3. **Read-only proxy access** (`kubectl-proxy-ord-devimprint:8001`)
   - Exists but CANNOT access secrets due to RBAC
   - Verification: `kubectl --server=http://kubectl-proxy-ord-devimprint:8001 get secret armor-writer -n devimprint`
   - Error: "User 'system:serviceaccount:devpod-observer:devpod-observer' cannot get resource 'secrets' in namespace 'devimprint'"

### Additional Issue: Field Name Mismatch

The task requests `LITESTREAM_ACCESS_KEY_ID`, but the `armor-writer` ExternalSecret defines different field names:

From `~/declarative-config/k8s/ord-devimprint/devimprint/devimprint-externalsecrets.yml` (lines 52-77):
```yaml
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: armor-writer
  namespace: devimprint
spec:
  target:
    name: armor-writer
  data:
    - secretKey: auth-access-key    # ← NOT LITESTREAM_ACCESS_KEY_ID
      remoteRef:
        key: rs-manager/ord-devimprint/armor-writer
        property: auth-access-key
    - secretKey: auth-secret-key    # ← NOT LITESTREAM_SECRET_ACCESS_KEY
      remoteRef:
        key: rs-manager/ord-devimprint/armor-writer
        property: auth-secret-key
```

This suggests either:
1. Task description uses outdated/wrong field names
2. A different secret needs to be accessed
3. Field names were renamed at some point

## Prerequisite Beads Status

The task lists prerequisites as complete:
- **bf-4743d**: "Verify kubeconfig path exists and is accessible" → closed (P2)
- **bf-2pn4n**: "Test kubectl access to devimprint namespace" → closed (P2)

However, verification shows their acceptance criteria are NOT met:
- bf-4743d requires file exists at `/home/coding/.kube/ord-devimprint.kubeconfig` → **file does not exist**
- bf-2pn4n requires kubectl can connect using kubeconfig → **cannot connect (file missing)**

## Acceptance Criteria - NOT MET

- [ ] Successfully executed kubectl command to retrieve LITESTREAM_ACCESS_KEY_ID field
- [ ] Command returned a value (not empty string)
- [ ] Value was captured (no command errors)

## What is Required to Complete

To complete this task, ONE of the following must be provided:

### Option 1: ord-devimprint Kubeconfig
Create kubeconfig at `/home/coding/.kube/ord-devimprint.kubeconfig` with secret read access:
```bash
kubectl --kubeconfig=/home/coding/.kube/ord-devimprint.kubeconfig \
  get secret armor-writer -n devimprint \
  -o jsonpath='{.data.auth-access-key}'  # Note: corrected field name
```

### Option 2: rs-manager Kubeconfig  
Create kubeconfig at `/home/coding/.kube/rs-manager.kubeconfig` with OpenBao access to retrieve credentials from source

### Option 3: Update RBAC
Grant the `devpod-observer` ServiceAccount secret read access on ord-devimprint cluster

### Option 4: Direct Credentials
Provide the actual base64-encoded values for:
- `auth-access-key`
- `auth-secret-key`

## Recommendation

**DO NOT CLOSE this bead.** The task cannot be completed without proper cluster access.

The prerequisite beads (bf-4743d, bf-2pn4n) appear to have been incorrectly marked as closed despite not meeting their acceptance criteria. The infrastructure blocker must be resolved before this task can proceed.

## Timestamp

Blocker confirmed: 2026-07-11

## References

- ExternalSecret config: `~/declarative-config/k8s/ord-devimprint/devimprint/devimprint-externalsecrets.yml`
- Cluster documentation: `CLAUDE.md` (Kubernetes Access section)
- Related blockers: Multiple git commits documenting same issue (4c018a22, 7ab9ab49, etc.)
