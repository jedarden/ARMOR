# Fixture rendered by barman_compat_test.go.  Credentials are intentionally
# placeholders: the test supplies its ephemeral pair at process runtime.
AWS_ENDPOINT_URL={{ENDPOINT}}
AWS_REGION={{REGION}}
AWS_ACCESS_KEY_ID={{ACCESS_KEY}}
AWS_SECRET_ACCESS_KEY={{SECRET_KEY}}
BARMAN_DESTINATION=s3://{{BUCKET}}/barman-compat
BARMAN_SERVER=armor-compat
BARMAN_MIN_CHUNK_SIZE=5MB
